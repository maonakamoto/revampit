#!/usr/bin/env sh
# Pre-commit: lint-staged runs ESLint + the Swiss-German umlaut linter on
# staged files only. Typecheck is intentionally NOT here — it's slow and
# CI catches it. To bypass in an emergency: `git commit --no-verify`.

npx lint-staged
