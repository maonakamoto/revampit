#!/usr/bin/env sh
# Production deploys run in GitHub Actions now:
#   .github/workflows/deploy-selfhost.yml  (push to main → build + deploy)
# It has `concurrency: cancel-in-progress`, so back-to-back pushes can't clobber
# each other's build the way this local background-deploy hook used to (it built
# in the working tree and raced on .next). The local auto-deploy is therefore
# disabled. To deploy manually from this machine in a pinch:
#   npm run deploy:selfhost
exit 0
